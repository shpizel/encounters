{% extends 'EncountersBundle:templates:layout.html.twig' %}
{% block title %}Взаимные{% endblock %}

{% block body %}
<div class="content mutual">

    {% if data == null %}
    <div class="none">
        <div class="title">Вы еще ни с кем не совпали</div>
        <div class="info">Как только кто-то ответит вам взаимностью, <br>мы бросим все дела и сразу же сообщим<br> вам об этом!<br><br><a href="/search" class="button long">Начните смотреть и оценивать прямо сейчас</a><div style="padding-top:15px;padding-bottom: 15px;">или</div><a class="ui-btn-big ui-btn">Купить 100 показов за 3<i class="coint"></i></a></div>
    </div>
    {% else %}
    <div class="pictures">
        <div class="title">Они ответили вам взаимностью</div>
        {% for item in data %}

        <div user_id="{{ item.info.oid }}" class="photoListItem" user_id="{{ item.info.oid }}">
            <div class="background"></div>
            <a class="close"></a>
            <img src="{{ item.info.medium_photo_url }}"><br>
            <div class="info"><a target="_blank" href="{{ platform.partner_url }}anketa.phtml?oid={{ item.info.oid }}">{{ item.info.name }}</a>&nbsp;<i class="ico {% if item.info.gender == 'F' %}female{% else %}male{% endif %}"></i>&nbsp;{% if item.info.age %}<b>{{ item.info.age }}</b>{% endif %}</div>
        </div>

        {% endfor %}
        <br clear="all">
        {% include 'EncountersBundle:tools:paginator.html.twig' %}
    </div>
    {% endif %}


</div>
{% include 'EncountersBundle:layers:layers.html.twig' %}
{% endblock %}

{% block pagejs %}
{% if json %}
{% autoescape false %}
$Config.set('users', {{ json }});
{% endautoescape %}
{% endif %}
{% endblock %}