{% extends 'EncountersBundle:templates:layout.html.twig' %}
{% block title %}Меня смотрели{% endblock %}

{% block body %}

    <div class="content visitors">

        {% if data == null %}
        <div class="none">
            <div class="title">Вас еще никто не смотрел</div>
            <div class="info">Мы показываем ваши фотографии другим людям так часто, <br>насколько вы оцениваете других людей!<br><br><a href="/search" class="button long">Начните смотреть и оценивать прямо сейчас</a>{#<div style="padding-top:15px;padding-bottom: 15px;">или</div><a class="ui-btn-big ui-btn">Получить 50 показов за 3<i class="coint"></i></a></div>#}
        </div>
        {% else %}
        <div class="pictures">
            <div class="title">Эти пользователи оценили вас&nbsp;&nbsp;<a href="#" class="ui-btn ui-btn-big" style="font-style: normal;font-family: Arial;margin-top:10px;">Получить 50 доп. оценок за 30<i class="account-heart"></i></a></div>
            {% for item in data %}

            {% if item.decision.1 == -2 %}
                <div user_id="{{ item.info.oid }}" small_photo_url="{{ item.info.small_photo_url }}" medium_photo_url="{{ item.info.medium_photo_url }}" name="{{ item.info.name }}" class="photoListItem closed{% if item.decision.2 == 1 %} new{% endif%}">
                    <div class="background"></div>
                    <img src="{{ item.info.medium_photo_url }}"><br>
                    <div class="info"><span class="name">{{ item.info.name }}</span> <i class="ico {% if item.info.gender == 'F' %}female{% else %}male{% endif %}"></i> {% if item.info.age %}<b>{{ item.info.age }}</b>{% endif %}<br></div>
                    <div class="link"><a class="ln" href="#"></a></div>
                </div>
            {% else %}
                <div user_id="{{ item.info.oid }}" small_photo_url="{{ item.info.small_photo_url }}" medium_photo_url="{{ item.info.medium_photo_url }}" name="{{ item.info.name }}" class="photoListItem{% if item.decision.1 == 0 %} maybe{% endif %}{% if item.decision.1 == 1 %} yes{% endif %}{% if item.decision.1 == -1 %} no{% endif %}">
                    <div class="background"></div>
                    <a class="profile-link" target="_top" href="{{ platform.partner_url }}app_platform/?action=view&app_id=355&extra=profile{{ item.info.oid }}"></a>
                    <img src="{{ item.info.medium_photo_url }}"><br>
                    <div class="info"><a target="_top" href="{{ platform.partner_url }}app_platform/?action=view&app_id=355&extra=profile{{ item.info.oid }}">{{ item.info.name }}</a>&nbsp;<i class="ico {% if item.info.gender == 'F' %}female{% else %}male{% endif %}"></i>&nbsp;{% if item.info.age %}<b>{{ item.info.age }}</b>{% endif %}</div>
                    <div class="link"><a class="ln" href="#"></a></div>
                </div>
            {% endif %}

            {% endfor %}
            <br clear="all">
            {% include 'EncountersBundle:tools:paginator.html.twig' %}
        </div>

        {% endif %}
    </div>
    {% include 'EncountersBundle:layers:layers.html.twig' %}

{% endblock %}

{% block pagejs %}
{% if json %}
{% autoescape false %}
$Config.set('users', {{ json }});
{% endautoescape %}
{% endif %}
{% endblock %}